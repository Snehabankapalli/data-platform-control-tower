services:
  control-tower:
    build: .
    ports:
      - "8000:8000"
    environment:
      APP_MODE: demo
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/health')"]
      interval: 15s
      timeout: 3s
      retries: 3

